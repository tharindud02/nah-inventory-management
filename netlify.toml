[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"

# Next.js: Netlify auto-detects and handles API routes via OpenNext.
# Do NOT add custom /api/* or /* redirects - they break Next.js routing.
